# Database Configuration
# PostgreSQL connection string for Render.com or local PostgreSQL
DATABASE_URL=postgresql://username:password@localhost:5432/steelworks_db

# Optional: Database connection parameters (used if DATABASE_URL not set)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=steelworks_db
DB_USER=postgres
DB_PASSWORD=postgres
